import SwiftUI

enum RelayDockColor {
    static let windowBackground = Color(nsColor: .windowBackgroundColor)
    static let sidebarBackground = Color(nsColor: .controlBackgroundColor)
    static let sidebarSelection = Color(nsColor: .unemphasizedSelectedContentBackgroundColor)
    static let contentBackground = Color(nsColor: .textBackgroundColor)
    static let controlBackground = Color(nsColor: .controlBackgroundColor)
    static let groupHeaderBackground = Color(nsColor: .controlBackgroundColor)
    static let rowHover = Color(nsColor: .unemphasizedSelectedContentBackgroundColor)
    static let subtleBorder = Color(nsColor: .separatorColor)
}
